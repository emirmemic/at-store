{
  "collectionName": "components_global_buttons",
  "info": {
    "displayName": "Button"
  },
  "options": {},
  "attributes": {
    "transparentVariant": {
      "type": "enumeration",
      "enum": ["blue_blue", "blue_black", "white", "white_blueBg", "black"]
    },
    "size": {
      "type": "enumeration",
      "enum": ["sm", "md", "lg", "xlg", "textWithIcon", "color"]
    },
    "typography": {
      "type": "enumeration",
      "enum": ["button1", "button2"]
    },
    "variant": {
      "type": "enumeration",
      "enum": [
        "filled",
        "transparent",
        "addToFavorites",
        "productVariant",
        "color"
      ]
    },
    "label": {
      "type": "string"
    }
  }
}
