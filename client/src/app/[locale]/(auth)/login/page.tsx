import LoginForm from '@/app/[locale]/(auth)/login/components/login-form';

export default function LoginPage() {
  return (
    <main className="flex items-center justify-center h-screen-h-cutoff">
      <LoginForm />
    </main>
  );
}
